version: '3.8'

services:
  postgres:
    image: postgres:15
    container_name: fee-postgres
    environment:
      POSTGRES_DB: fee_db
      POSTGRES_USER: user
      POSTGRES_PASSWORD: password
    ports:
      - "5433:5432" # avoid conflicts with other services