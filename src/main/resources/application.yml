spring:
  datasource:
    url: ${DB_URL}
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

fee:
  rate: ${FEE_RATE:0.01}

internal:
  api-key: ${INTERNAL_API_KEY}
